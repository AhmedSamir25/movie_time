import 'package:flutter/material.dart';

class HomeDetilsView extends StatelessWidget {
  const HomeDetilsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}